(function(g){'use strict';async function fit(src){const im=new Image();im.src=src;await im.decode();const c=document.createElement('canvas'),scale=Math.min(1,2000/Math.max(im.width,im.height));c.width=Math.max(1,Math.round(im.width*scale));c.height=Math.max(1,Math.round(im.height*scale));const ctx=c.getContext('2d');ctx.drawImage(im,0,0,c.width,c.height);const data=ctx.getImageData(0,0,c.width,c.height).data;let x0=c.width,y0=c.height,x1=-1,y1=-1;let transparent=0;for(let i=3;i<data.length;i+=4)if(data[i]<16)transparent++;const alphaOnly=transparent>c.width*c.height*.01;for(let y=0;y<c.height;y++)for(let x=0;x<c.width;x++){const i=(y*c.width+x)*4;if(data[i+3]>24&&(alphaOnly||Math.min(data[i],data[i+1],data[i+2])<242)){x0=Math.min(x0,x);y0=Math.min(y0,y);x1=Math.max(x1,x);y1=Math.max(y1,y);}}if(x1<0)return{data:src,ratio:im.width/im.height};const margin=Math.max(2,Math.round(Math.max(x1-x0,y1-y0)*.015));x0=Math.max(0,x0-margin);y0=Math.max(0,y0-margin);x1=Math.min(c.width-1,x1+margin);y1=Math.min(c.height-1,y1+margin);const out=document.createElement('canvas');out.width=x1-x0+1;out.height=y1-y0+1;out.getContext('2d').drawImage(c,x0,y0,out.width,out.height,0,0,out.width,out.height);return{data:out.toDataURL('image/png'),ratio:out.width/out.height};}
async function apply(p,src){if(!src&&(!p.meta.logo||p.meta.logoFitVersion===1))return;if(src)p.meta.logoOriginal=src;else p.meta.logoOriginal??=p.meta.logo;const r=await fit(p.meta.logoOriginal);p.meta.logo=r.data;p.meta.logoRatio=r.ratio;p.meta.logoFitVersion=1;}
g.LSLogo={fit,apply};})(typeof window!=='undefined'?window:globalThis);
