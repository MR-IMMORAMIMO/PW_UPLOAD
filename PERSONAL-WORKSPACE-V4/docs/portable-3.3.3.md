# Portable 3.3.3 — review controls visibility

Fixes the Technical Verification inspector clipping after 3.3.2. Field details, evidence, review, and editing use numbered pages with persistent Review reading / Edit value shortcuts. A bounded overflow fallback keeps expanded forms reachable on short windows. Navigator capacity is measured from the actual modal list height; cards no longer compress or overlap.

Output Studio settings now scroll independently with every column control present. Settings pagination is removed; document preview geometry is preserved. No project data, extraction logic, Library ownership, or history contracts change. No AI model is included.

Validation: Technical Check UI suite; full V4 UI suite; TypeScript; scoped ESLint and formatting; packaged Electron synthetic wrong-identity review with nine luminaires at 1920x1080 and 1280x800; saved field verified through the API; final Output control reached by scrolling; Datasheet reference image and evidence smoke tests. Exact results and release hashes are in the release validation folder. Tests use disposable profiles and projects.

Run `SCT Workspace Portable v3.3.3.exe` from the same portable folder as the existing version to retain the adjacent Workspace Data. Close the earlier application first; retain the older executable as a fallback. No commit or push was made.
