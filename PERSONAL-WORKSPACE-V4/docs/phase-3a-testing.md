# Phase 3A testing

Phase 3A tests use disposable roots only. They must never use Golden, MADAM OLD PALACE, the Owner Personal Workspace database, or business/OneDrive Project folders.

Focused coverage includes:

- shared Create/Edit normalization, required/date/hours validation, commercial pairs, scope and input mode;
- atomic Personal Project configuration commit and rollback;
- Floating Workspace sections, unchanged Save, continuous typing/deletion focus, dirty Escape interception, and focus containment/return;
- Header status direct-target filtering, optimistic update, server reconciliation, and stale-state rollback;
- strict marker health, read-only legacy classification, explicit initial binding/adoption, wrong-Project rejection, other-Project binding rejection, permission/IO derivation, and newly-created-marker compensation;
- verified-root failure before representative file mutation routes.

The schema remains v19. No migration or live UAT is part of this validation slice.
